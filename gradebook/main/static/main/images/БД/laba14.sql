USE ELSCDnevnik;
-- create UNIQUE index FullnameOfStudents on students(FullName);
-- create UNIQUE index FullnameOfTeachers on classadmins(FullName);
-- create index Classes on classes(ClassAdmin, Cabinets);
select * from classadmins;